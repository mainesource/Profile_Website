console.log('Javascript is loaded');

function changeTitleColor(color) {
  const titleElement = document.getElementsByClassName("title")
  for(let i = 0; i < titleElement.length; i++){
    titleElement[i].computedStyleMap.color;
  }    
}

